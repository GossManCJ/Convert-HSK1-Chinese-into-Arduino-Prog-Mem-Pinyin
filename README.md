# What do?
Python file reads JSON file provided from https://github.com/nicolas-jaussaud/hsk-words
I then printed out a format to copy paste into Arduino IDE which I can then use to display random HSK1 words (incorporated into existing Arduino code) on a OLED panel
